_BASE_: '../detr_graphdense.yaml'

DATASETS:
  TRAIN: ("mem_option2a_mapper_smoke",)
  TEST: ("mem_option2a_mapper_smoke",)
  EVAL: ()
  MEM_RECORDS_JSON: ""
  MEM_MAX_RECORDS: 5

INPUT:
  GRAPH_GT_TYPE: "dense"
  MEM_OBSERVED_VIEW_PROTOCOL: "uniform10"
  MEM_MAX_SELECTED_VIEWS: 10
  MEM_EXPECTED_HEIGHT: 140
  MEM_EXPECTED_WIDTH: 200
  MEM_SEMANTIC_CLASS_MIN: 0
  MEM_SEMANTIC_CLASS_MAX: 14
  MEM_VALIDATE_SEMANTIC_RANGE: True

MODEL:
  META_ARCHITECTURE: "GraphDetr"
  MASK_ON: False
  WEIGHTS: ""

SOLVER:
  IMS_PER_BATCH: 1

DATALOADER:
  NUM_WORKERS: 0
